
local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "Balli Hood Gui Key System", HidePremium = false, SaveConfig = true, ConfigFolder = "Key"})

local PlayerTab = Window:MakeTab({
    Name = "Key system",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local KeySection = PlayerTab:AddSection({
    Name = "Key System"
})

local KeyTextbox = KeySection:AddTextbox({
    Name = "Enter Key",
    Callback = function(Value)
        local keys = {"badguy", "tangina", "tangina"}
        local isKeyCorrect = false

        for i, v in pairs(keys) do
            if Value == v then
                isKeyCorrect = true
                break
            end
        end

        if isKeyCorrect then
 local Sound = Instance.new("Sound",game:GetService("SoundService"))
Sound.SoundId = "rbxassetid://3094993009"
Sound:Play()
-- [[ fnl example ]] --

-- Import the Library
local fnl = loadstring(game:HttpGetAsync 'https://raw.githubusercontent.com/Code1Tech/utils/main/notification.lua')()

-- Make a Notification function
function notify(title, text, duration)
  title = title or "Notification"
  text = text or "No text provided."
  duration = duration or 5
  
  fnl:MakeNotification({
    Title = title,
    Text = text,
    Duration = duration
  })
end

-- Use the func
notify("Key System", "Checking The Key...", 3)
wait(1.5)
           -- [[ fnl example ]] --

-- Import the Library
local fnl = loadstring(game:HttpGetAsync 'https://raw.githubusercontent.com/Code1Tech/utils/main/notification.lua')()

-- Make a Notification function
function notify(title, text, duration)
  title = title or "Notification"
  text = text or "No text provided."
  duration = duration or 5
  
  fnl:MakeNotification({
    Title = title,
    Text = text,
    Duration = duration
  })
end

-- Use the func
local Sound = Instance.new("Sound",game:GetService("SoundService"))
Sound.SoundId = "rbxassetid://1584394759"
Sound:Play()
notify("Key System", "Key Correct!", 3)
OrionLib:Destroy()
loadstring(game:HttpGet("https://raw.githubusercontent.com/YouWillNeverFindMeCuzItsVeryHardToDoIt/YOUCANEVENFINDMBROITSVERYHARDHAHA/main/Ito_PoMain"))()
        else
        local Sound = Instance.new("Sound",game:GetService("SoundService"))
Sound.SoundId = "rbxassetid://3094993009"
Sound:Play()
-- [[ fnl example ]] --

-- Import the Library
local fnl = loadstring(game:HttpGetAsync 'https://raw.githubusercontent.com/Code1Tech/utils/main/notification.lua')()

-- Make a Notification function
function notify(title, text, duration)
  title = title or "Notification"
  text = text or "No text provided."
  duration = duration or 5
  
  fnl:MakeNotification({
    Title = title,
    Text = text,
    Duration = duration
  })
end

-- Use the func
notify("Key System", "Checking The Key...", 3)
wait(1.5)
                   -- [[ fnl example ]] --

-- Import the Library
local fnl = loadstring(game:HttpGetAsync 'https://raw.githubusercontent.com/Code1Tech/utils/main/notification.lua')()

-- Make a Notification function
function notify(title, text, duration)
  title = title or "Notification"
  text = text or "No text provided."
  duration = duration or 5
  
  fnl:MakeNotification({
    Title = title,
    Text = text,
    Duration = duration
  })
end

-- Use the func
local Sound = Instance.new("Sound",game:GetService("SoundService"))
Sound.SoundId = "rbxassetid://136075117"
Sound:Play()
notify("Key System", "Incorrect Key / Key Expired!", 3)
        end
    end
})

KeySection:AddButton({
	Name = "Get Key",
	Callback = function()
OrionLib:MakeNotification({
	Name = "Key System",
	Content = "Key has been copied to your clipboard.",
	Image = "rbxassetid://4483345998",
	Time = 5
})
      		setclipboard("script updating please wait")
  	end    
})

KeySection:AddButton({
	Name = "How To Get Key",
	Callback = function()
OrionLib:MakeNotification({
	Name = "How To Get Key",
	Content = "The link has been copied to your clipboard.",
	Image = "rbxassetid://4483345998",
	Time = 5
})
      		setclipboard("https://youtu.be/Iyf-I5HuL_Q")
  	end    
})

KeySection:AddButton({
	Name = "Discord Server",
	Callback = function()
OrionLib:MakeNotification({
	Name = "Discord Server",
	Content = "The link has been copied to your clipboard.",
	Image = "rbxassetid://4483345998",
	Time = 5
})
      		setclipboard("https://discord.gg/vzF3zHcNeU")
  	end    
})

KeySection:AddButton({
	Name = "Youtube Channel",
	Callback = function()
OrionLib:MakeNotification({
	Name = "Youtube Channel",
	Content = "The link has been copied to your clipboard.",
	Image = "rbxassetid://4483345998",
	Time = 5
})
      		setclipboard("https://youtube.com/@BalligusapoYT")
  	end    
})

-- [[ fnl example ]] --

-- Import the Library
local fnl = loadstring(game:HttpGetAsync 'https://raw.githubusercontent.com/Code1Tech/utils/main/notification.lua')()

-- Make a Notification function
function notify(title, text, duration)
  title = title or "Notification"
  text = text or "No text provided."
  duration = duration or 5
  
  fnl:MakeNotification({
    Title = title,
    Text = text,
    Duration = duration
  })
end

-- Use the func
notify("Key Changed!", "July 28, 2023", 3)
